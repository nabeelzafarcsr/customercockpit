_schema-version: "3.2"
ID: comcsrcustomercockpit
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comcsrcustomercockpit-destination-content
  type: com.sap.application.content
  requires:
  - name: comcsrcustomercockpit-destination-service
    parameters:
      content-target: true
  - name: comcsrcustomercockpit-repo-host
    parameters:
      service-key:
        name: comcsrcustomercockpit-repo-host-key
  - name: comcsrcustomercockpit-uaa
    parameters:
      service-key:
        name: comcsrcustomercockpit-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comcsrcustomercockpit_html_repo_host
          ServiceInstanceName: comcsrcustomercockpit-html5-service
          ServiceKeyName: comcsrcustomercockpit-repo-host-key
          sap.cloud.service: comcsrcustomercockpit
        - Authentication: OAuth2UserTokenExchange
          Name: comcsrcustomercockpit_uaa
          ServiceInstanceName: comcsrcustomercockpit-xsuaa-service
          ServiceKeyName: comcsrcustomercockpit-uaa-key
          sap.cloud.service: comcsrcustomercockpit
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: comcsrcustomercockpit-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comcsrcustomercockpit-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comcsrcustomercockpit.zip
      name: comcsrcustomercockpit
      target-path: resources/
- name: comcsrcustomercockpit
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comcsrcustomercockpit-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comcsrcustomercockpit-destination-service
    service-plan: lite
- name: comcsrcustomercockpit-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: comcsrcustomercockpit-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-name: comcsrcustomercockpit-xsuaa-service
    service-plan: application
- name: comcsrcustomercockpit-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comcsrcustomercockpit-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
